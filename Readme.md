# Flow Log Parser

This project processes flow logs and maps them to tags using a lookup table. It generates outputs that summarize tag counts and port/protocol combination counts, helping analyze network traffic effectively.

## Features
- Parse lookup tables for ports, protocols, and tags.
- Process flow logs to extract relevant fields.
- Map flow log entries to tags based on the lookup table.
- Generate output summarizing tag counts and port/protocol combinations.
- Error handling for missing files and invalid data.

---

## Prerequisites
- Python 3.8+
- `unittest` for testing.

---

## File Structure
```
project/
├── main.py       # Contains the main logic.
├── tests/
│   ├── test_flow_logs.py # Unit tests for the script.
├── lookup_tables.csv    # Sample lookup table file.
├── flow_logs.txt        # Sample flow logs file.
└── output.csv           # Output file generated by the script.
```

---

## Usage

### Step 1: Input Files
Prepare the following input files:
1. **Lookup Table** (`lookup_tables.csv`):
   - Format:
     ```csv
     dstport,protocol,tag
     80,tcp,HTTP
     443,tcp,HTTPS
     53,udp,DNS
     ```
2. **Flow Logs** (`flow_logs.txt`):
   - Format:
     ```
     2 123 eni-0ab 192.168.1.1 10.0.0.1 12345 80 6 1000 2000 1620140761 1620140821 ACCEPT OK
     2 123 eni-0ab 192.168.1.2 10.0.0.2 54321 443 6 1500 3000 1620140761 1620140821 ACCEPT OK
     ```

### Step 2: Run the Script
Execute the script:
```bash
python3 main.py
```
This will generate an output file named `output.csv` containing the tag counts and port/protocol combination counts.

---

## Output
The output file `output.csv` contains:
1. **Tag Counts**:
   ```
   Tag Counts:
   Tag,Count
   http,1
   https,2
   untagged,1
   ```
2. **Port/Protocol Combination Counts**:
   ```
   Port/Protocol Combination Counts:
   Port,Protocol,Count
   80,tcp,1
   443,tcp,2
   8080,tcp,1
   ```

---

## Testing
Unit tests are included in the `test_flow_logs.py` file. To run the tests:
```bash
python3 -m unittest discover tests
```

---

## Error Handling
- **Missing Files**: Raises `FileNotFoundError` if input files are not found.
- **Invalid Data**: Logs warnings for invalid entries or fields.

---



---


## Protocol Numbers 
Assigned based on http://www.iana.org/assignments/protocol-numbers/protocol-numbers.xhtml#protocol-numbers-1
